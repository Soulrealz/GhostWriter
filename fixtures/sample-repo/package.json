{
  "name": "sample-repo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Target repo for agent runs. Zero dependencies on purpose.",
  "scripts": {
    "test": "node --test"
  }
}
